<h1>Books <small>(<%= @author.name %>)</small></h1>

<p>
  <%= link_to "Add Book", new_author_book_path %>
</p>

<table border="1">
  <tr>
    <th>
      Title
    </th>
  </tr>
  <% @books.each do |book| %>
    <tr>
      <td>
        <%= book.title %>
      </td>
    </tr>
  <% end %>
</table>
